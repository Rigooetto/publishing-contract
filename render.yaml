services:
  - type: web
    name: labelmind
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 3600 --graceful-timeout 600 --worker-class gthread --workers 2 --threads 4
    stopTimeout: 600
